export type Game = {
    name: string;
    image: string;
    salePrice: number;
    price: number;
    sale: string;
    tags: string[];
};

export type Article = {
    name: string;
    image: string;
    description: string;
    posted_at: string;
}
